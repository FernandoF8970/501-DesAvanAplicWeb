/*
/// <reference types="cypress" />
import { mount } from 'cypress/react'
import TravelRequestForm from '../../class4/A01028796/TravelRequestForm'

describe('TravelRequestForm - Prueba Unitaria', () => {
  it('renderiza los campos correctamente', () => {
    mount(<TravelRequestForm />)
    cy.get('input[placeholder="Destination"]').should('exist')
    cy.get('input[placeholder="Start Date"]').should('exist')
    cy.get('input[placeholder="End Date"]').should('exist')
    cy.get('textarea[placeholder="Purpose"]').should('exist')
  })
})
*/